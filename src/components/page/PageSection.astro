---
import type {
	HomeHeroFragment,
	InnerHeroFragment,
	ArticleHeroFragment,
	FormFragment
} from '../../generated/graphql'
import InnerHero from '../hero/InnerHero.astro'
import HomeHero from '../hero/HomeHero.astro'
import ArticleHero from '../hero/ArticleHero.astro'
import Form from '../form/Form.astro'

type Props = {
	section: HomeHeroFragment | InnerHeroFragment | ArticleHeroFragment | FormFragment
}

const { section } = Astro.props
---

<switch values={section.__typename}>
	<case value="HomeHeroRecord">
		<HomeHero {...section} />
	</case>

	<case value="InnerHeroRecord">
		<InnerHero {...section} />
	</case>

	<case value="ArticleHeroRecord">
		<ArticleHero {...section} />
	</case>

	<case value="FormRecord">
		<Form {...section as FormFragment} />
	</case>
</switch>
